---
phase: 02-production-testing
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02"]
files_modified:
  - .planning/phases/02-production-testing/02-04-SUMMARY.md
  - .planning/STATE.md
autonomous: false

must_haves:
  truths:
    - "Two users can collaborate via notes on a transaction"
    - "Transaction flags (NEEDS_REVIEW, APPROVED) can be set"
    - "Organization-scoped messages work between users"
    - "Resend email attempt is logged (even if delivery fails due to domain verification)"
  artifacts:
    - path: ".planning/phases/02-production-testing/02-04-SUMMARY.md"
      provides: "Test results for collaboration and email, plus full phase summary"
---

<objective>
Test collaboration features (notes, messages, flags) and Resend email verification on the live production URL. Produce final phase summary.

Purpose: Collaboration requires two accounts and organization membership, making it the most complex test block. Resend is a known soft blocker — we verify the attempt, not delivery. This plan concludes Phase 02.
Output: Test log with pass/fail/blocked, plus final phase-wide test summary with all results.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-testing/02-RESEARCH.md
@.planning/phases/02-production-testing/02-01-SUMMARY.md
@.planning/phases/02-production-testing/02-02-SUMMARY.md
@.planning/phases/02-production-testing/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Collaboration Features (Notes, Messages, Flags)</name>
  <files>.planning/phases/02-production-testing/02-04-SUMMARY.md</files>
  <action>
    Guide the user through collaboration feature verification. Requires two accounts (CLIENT + ACCOUNTANT from Plan 01) and at least one transaction in the database (from Plan 02).

    ORGANIZATION CHECK: Collaboration features require an organization.
    1. Sign in as the ACCOUNTANT user
    2. Check if a Clerk organization was created (Clerk Dashboard > Organizations, or check Supabase organizationMember table)
    3. If no organization exists, create one in Clerk Dashboard and add both users

    TEST 7.1 — Add note to transaction:
    1. Sign in as ACCOUNTANT
    2. Navigate to a transaction (from the synced Plaid transactions)
    3. Add a note to the transaction (use UI or POST to /api/collaboration/notes with { transactionId, content })
    4. Expected: Note appears attached to the transaction

    TEST 7.2 — Reply to note (threaded):
    1. Sign in as CLIENT (different browser/incognito)
    2. Navigate to the same transaction
    3. Reply to the note from 7.1
    4. Expected: Reply appears as a threaded response

    TEST 7.3 — Flag a transaction:
    1. As ACCOUNTANT, flag a transaction as NEEDS_REVIEW
    2. Expected: Flag status visible on the transaction
    3. Then change to APPROVED
    4. Expected: Flag status updates

    TEST 7.4 — Send organization message:
    1. As ACCOUNTANT, send a message via the messages feature
    2. Switch to CLIENT account, check messages
    3. Expected: Message visible to both users in the same organization

    TEST 7.5 — Notifications endpoint:
    1. Check if the notifications endpoint responds: /api/collaboration/notifications
    2. Expected: Returns a response (may be empty array if Resend is blocked)
  </action>
  <verify>User reports PASS/FAIL/BLOCKED for Organization check and Tests 7.1 through 7.5</verify>
  <done>Collaboration features verified: notes, threaded replies, flags, and messages all working between two users (or failures documented with specific errors).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Resend Email Verification + Phase Summary</name>
  <files>.planning/phases/02-production-testing/02-04-SUMMARY.md, .planning/STATE.md</files>
  <action>
    Guide the user through Resend email verification and compile the final phase summary.

    TEST 8.1 — Check Resend domain status:
    1. Go to Resend Dashboard (https://resend.com/domains)
    2. Check domain verification status for finclear.app (or whichever domain is configured)

    TEST 8.2 — Verify email attempt in logs:
    1. Go to Resend Dashboard > Logs (or Emails)
    2. Look for any email send attempts from the testing done in Plans 01-04
    3. Expected: At least one send attempt logged (from signup or other trigger)
    4. If domain is verified: Check if email was actually delivered to inbox
    5. If domain is pending: Confirm the attempt was logged but delivery failed (expected)

    TEST 8.3 — Application handles email failure gracefully:
    1. Verify that no feature was broken by email send failures
    2. Signup should work even if welcome email fails
    3. Collaboration should work even if notification email fails

    FINAL PHASE SUMMARY:
    After completing these tests, compile a summary of ALL test results from Plans 01 through 04:
    - Total tests: [count]
    - PASS: [count]
    - FAIL: [count with list]
    - BLOCKED: [count with list and reasons]
    - SKIPPED: [count with list]

    List any bugs discovered that need fixing in Phase 3 or a gap closure plan. Update STATE.md to reflect Phase 02 completion status.
  </action>
  <verify>User reports domain status, PASS/FAIL/BLOCKED for Tests 8.1-8.3, and confirms the final phase summary</verify>
  <done>Resend status documented. Full phase test results compiled across all 4 plans. STATE.md updated with Phase 02 outcome.</done>
</task>

</tasks>

<verification>
- Collaboration features tested with two users in same organization
- Resend email status documented (verified, pending, or not configured)
- Application handles email failures gracefully (no cascading errors)
- Complete phase test results compiled across all 4 plans
</verification>

<success_criteria>
- All 8 test cases in this plan executed with documented results
- Full phase summary compiled with pass/fail/blocked counts across all 30+ tests
- Any bugs discovered during testing are documented with specific details
- STATE.md updated to reflect Phase 02 completion status
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-testing/02-04-SUMMARY.md` with all test results and the phase-wide summary.
</output>
