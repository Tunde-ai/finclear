---
phase: 02-production-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-production-testing/02-01-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "New user can sign up, select CLIENT role, and land on /dashboard/client"
    - "New user can sign up, select ACCOUNTANT role, and land on /dashboard/accountant"
    - "Returning user is routed to correct dashboard without re-onboarding"
    - "Cross-role access is blocked by middleware"
    - "Jamaica House dashboard loads and accepts data entry"
  artifacts:
    - path: ".planning/phases/02-production-testing/02-01-SUMMARY.md"
      provides: "Test results for auth, onboarding, and Jamaica House"
---

<objective>
Test Auth/Onboarding flow and Jamaica House module on the live production URL.

Purpose: Verify the two independent feature groups that have zero external dependencies (no Plaid, Stripe, or Supabase Storage needed). Auth must work before any other feature can be tested. Jamaica House is fully standalone.
Output: Test log with pass/fail/blocked for each test case.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-testing/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Auth + Onboarding Flow Verification</name>
  <files>.planning/phases/02-production-testing/02-01-SUMMARY.md</files>
  <action>
    Guide the user through auth and onboarding verification on the live production URL. This is a manual testing task — no code changes expected. All auth and onboarding features were built in prior phases. Document results in the SUMMARY file.

    Execute each test in order on https://finclear.app. Record pass/fail/blocked for each.

    TEST 1.1 — New CLIENT signup:
    1. Open https://finclear.app/sign-up in a browser (or incognito window)
    2. Create a new account with a real email address
    3. After signup, you should be redirected to /onboarding
    4. Select "CLIENT" role
    5. Expected: Redirect to /dashboard/client

    TEST 1.2 — New ACCOUNTANT signup:
    1. Open https://finclear.app/sign-up in a DIFFERENT browser or incognito window
    2. Create a second account with a different email
    3. After signup, should land on /onboarding
    4. Select "ACCOUNTANT" role
    5. Expected: Redirect to /dashboard/accountant

    TEST 1.3 — Returning user sign-in:
    1. Sign out, then go to https://finclear.app/sign-in
    2. Sign in with the CLIENT account from Test 1.1
    3. Expected: Direct redirect to /dashboard/client (NOT /onboarding)

    TEST 1.4 — Cross-role protection:
    1. While signed in as CLIENT, manually navigate to https://finclear.app/dashboard/accountant
    2. Expected: Redirected back to /dashboard/client

    NOTE: If Test 1.1 or 1.2 fails with a redirect loop between /onboarding and /dashboard, this is Pitfall 6 from research — try hard-refreshing the page. Report whether hard-refresh resolves it.
  </action>
  <verify>User reports PASS/FAIL/BLOCKED for each of Tests 1.1 through 1.4</verify>
  <done>All 4 auth tests have documented results. CLIENT and ACCOUNTANT accounts exist for use in subsequent plans.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Jamaica House Module Verification</name>
  <files>.planning/phases/02-production-testing/02-01-SUMMARY.md</files>
  <action>
    Guide the user through Jamaica House module verification. Jamaica House is a standalone module with MarketSale, AdSpend, and OperatingCost models. Dashboard at /dashboard/jamaica-house.

    Execute each test on https://finclear.app while signed in as the CLIENT user.

    TEST 2.1 — Jamaica House dashboard loads:
    1. Navigate to https://finclear.app/dashboard/jamaica-house
    2. Expected: Page loads without errors (may show empty state — that is OK)

    TEST 2.2 — Enter sales data:
    1. Use the Jamaica House UI to add a market sale record
    2. If UI does not have a form, use browser console or API client to POST:
       POST https://finclear.app/api/jamaica-house/sales
       Body: { "date": "2026-03-13", "location": "Test Market", "revenue": 500, "unitsSold": 50, "product": "Hot Sauce" }
    3. Expected: Record created successfully, appears in dashboard

    TEST 2.3 — Enter ad spend data:
    1. Add an ad spend record via UI or POST to /api/jamaica-house/ads
       Body: { "date": "2026-03-13", "platform": "Instagram", "spend": 100, "impressions": 5000, "clicks": 200 }
    2. Expected: Record created, visible in dashboard

    TEST 2.4 — Enter operating costs:
    1. Add an operating cost record via UI or POST to /api/jamaica-house/costs
       Body: { "date": "2026-03-13", "category": "Ingredients", "amount": 150, "description": "Peppers and spices" }
    2. Expected: Record created, visible in dashboard

    TEST 2.5 — Dashboard renders data:
    1. After entering data in 2.2-2.4, refresh /dashboard/jamaica-house
    2. Expected: Dashboard shows the entered data in charts/tables
  </action>
  <verify>User reports PASS/FAIL/BLOCKED for each of Tests 2.1 through 2.5</verify>
  <done>All 5 Jamaica House tests have documented results. Module confirmed working or failures documented with details.</done>
</task>

</tasks>

<verification>
- Auth flow tests 1.1-1.4 all PASS (or documented FAIL with details)
- Jamaica House tests 2.1-2.5 all PASS (or documented FAIL with details)
- Both user accounts (CLIENT + ACCOUNTANT) exist and are accessible for subsequent plans
</verification>

<success_criteria>
- All 9 test cases executed with documented results
- CLIENT and ACCOUNTANT accounts created and functional
- Jamaica House module verified as working or documented as broken
- Any failures have specific details (error messages, screenshots, URLs)
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-testing/02-01-SUMMARY.md` with all test results.
</output>
