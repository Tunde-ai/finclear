---
phase: 02-production-testing
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-production-testing/02-02-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Plaid Link opens and connects to sandbox bank (First Platypus Bank)"
    - "Accounts appear in dashboard after Plaid connection"
    - "Transactions are synced and visible in the Transactions view"
    - "Document upload to Supabase Storage succeeds for allowed file types"
    - "Oversized files are rejected with an appropriate error"
  artifacts:
    - path: ".planning/phases/02-production-testing/02-02-SUMMARY.md"
      provides: "Test results for Plaid, transactions, and document upload"
---

<objective>
Test Plaid bank connection, transaction sync, and document upload on the live production URL.

Purpose: Plaid connection + transactions are the critical prerequisite for AI report generation (Plan 03). Document upload tests Supabase Storage integration. Both require auth (Plan 01).
Output: Test log with pass/fail/blocked for each test case. Transactions must exist in DB for Plan 03.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-testing/02-RESEARCH.md
@.planning/phases/02-production-testing/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Plaid Bank Connection + Transaction Sync</name>
  <files>.planning/phases/02-production-testing/02-02-SUMMARY.md</files>
  <action>
    Guide the user through Plaid bank connection and transaction sync verification. Uses Plaid sandbox credentials.

    PREREQUISITE CHECK: Before testing, verify Plaid env vars in Vercel Dashboard:
    - PLAID_ENV must be "sandbox" (not "production")
    - PLAID_CLIENT_ID and PLAID_SECRET must be sandbox credentials
    - If PLAID_ENV is "production" but you only have sandbox keys, temporarily change to "sandbox" in Vercel and redeploy

    Execute each test on https://finclear.app while signed in as the CLIENT user from Plan 01.

    TEST 3.1 — Plaid Link opens:
    1. Navigate to the accounts/bank connection section in the client dashboard
    2. Click the button to connect a bank account
    3. Expected: Plaid Link modal opens
    4. If error: Check browser console for "create-link-token" API errors

    TEST 3.2 — Sandbox bank connection:
    1. In the Plaid Link modal, search for "First Platypus Bank"
    2. Enter credentials: Username: user_good, Password: pass_good
    3. Follow prompts to complete connection
    4. Expected: Modal closes, accounts appear in dashboard

    TEST 3.3 — Accounts visible in dashboard:
    1. After Plaid connection completes, check the accounts section
    2. Expected: At least one account with a balance displayed
    3. Verify via Supabase Dashboard: check the `account` table for rows belonging to your user

    TEST 3.4 — Transactions synced:
    1. Navigate to the Transactions view in the client dashboard
    2. Expected: Transactions populated (Plaid sandbox returns ~90 days of synthetic transactions)
    3. Verify via Supabase Dashboard: check the `transaction` table for rows
    4. IMPORTANT: If transactions show 0 rows despite accounts appearing, this is Pitfall 8 — check if personal_finance_category is null in sandbox responses. Report this specifically.

    TEST 3.5 — Balances endpoint:
    1. If the dashboard shows account balances, this is already verified
    2. Alternatively, in browser console: fetch('/api/plaid/balances').then(r => r.json()).then(console.log)
    3. Expected: JSON response with account balances
  </action>
  <verify>User reports PASS/FAIL/BLOCKED for PLAID_ENV check and Tests 3.1 through 3.5</verify>
  <done>Plaid connected via sandbox, accounts visible, transactions synced to DB (or failures documented). Transaction data exists for AI report testing in Plan 03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Document Upload + Supabase Storage</name>
  <files>.planning/phases/02-production-testing/02-02-SUMMARY.md</files>
  <action>
    Guide the user through document upload and Supabase Storage verification.

    PREREQUISITE CHECK: Before testing, verify the Supabase Storage bucket exists:
    1. Go to Supabase Dashboard > Storage
    2. Look for a bucket (check lib/supabase.ts for the exact bucket name — likely "documents")
    3. If bucket does NOT exist, create it in Supabase Dashboard before proceeding

    Execute each test on https://finclear.app while signed in as the CLIENT user.

    TEST 4.1 — Upload a PDF:
    1. Navigate to the Documents section in the client dashboard
    2. Upload a small PDF file (any PDF, under 25MB)
    3. Expected: Upload succeeds, document appears in the documents list

    TEST 4.2 — Upload an image (JPG or PNG):
    1. Upload a JPG or PNG image file
    2. Expected: Upload succeeds, document appears in list

    TEST 4.3 — Verify file in Supabase Storage:
    1. Go to Supabase Dashboard > Storage > [bucket name]
    2. Expected: Uploaded files visible in the bucket with path format {userId}/{timestamp}-{filename}

    TEST 4.4 — Oversized file rejection:
    1. Attempt to upload a file larger than 25MB (if you have one available)
    2. Expected: Upload rejected with an error message about file size
    3. If no large file available, mark as SKIPPED

    TEST 4.5 — PDF parsing status:
    1. For the PDF uploaded in 4.1, check if its parsedStatus changed
    2. The document record should show parsedStatus transitioning from PARSING to PARSED or FAILED
    3. This may be visible in the UI or in Supabase Dashboard > Table editor > document table
  </action>
  <verify>User reports PASS/FAIL/BLOCKED/SKIPPED for Bucket check and Tests 4.1 through 4.5</verify>
  <done>Document upload verified working or documented as broken. Supabase Storage bucket confirmed to exist.</done>
</task>

</tasks>

<verification>
- Plaid sandbox bank connected and transactions visible in dashboard
- At least 1 document uploaded to Supabase Storage successfully
- Transaction data exists in DB (prerequisite for AI reports in Plan 03)
</verification>

<success_criteria>
- All 10 test cases executed with documented results
- Plaid connection and transaction sync confirmed working (or documented failure)
- Document upload confirmed working (or documented failure with Supabase bucket status)
- Any failures have specific error messages and reproduction steps
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-testing/02-02-SUMMARY.md` with all test results.
</output>
