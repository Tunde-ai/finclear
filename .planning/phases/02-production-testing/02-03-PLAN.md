---
phase: 02-production-testing
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-production-testing/02-03-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "AI report generates with streaming text visible in real-time"
    - "Generated report reaches COMPLETED status"
    - "PDF export downloads a valid PDF file"
    - "Shared report link works in unauthenticated browser session"
    - "Stripe checkout completes with test card and subscription is created"
    - "Stripe billing portal loads for subscribed user"
  artifacts:
    - path: ".planning/phases/02-production-testing/02-03-SUMMARY.md"
      provides: "Test results for AI reports, PDF, share links, and Stripe"
---

<objective>
Test AI report generation, PDF export, shared report links, and Stripe checkout/subscription on the live production URL.

Purpose: AI reports require transactions from Plan 02. Stripe checkout is independent of Plaid but sequenced here to keep plan count manageable. Both are core revenue/value features.
Output: Test log with pass/fail/blocked for each test case.
</objective>

<execution_context>
@/Users/rfmstaff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rfmstaff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-testing/02-RESEARCH.md
@.planning/phases/02-production-testing/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: AI Report Generation + PDF Export + Share Links</name>
  <files>.planning/phases/02-production-testing/02-03-SUMMARY.md</files>
  <action>
    Guide the user through AI report generation, PDF export, and shared report link verification.

    PREREQUISITE: Transactions must exist in the database from Plan 02 (Plaid sync). If Plaid tests failed, these tests will be BLOCKED.

    Execute each test on https://finclear.app while signed in as the CLIENT user.

    TEST 5.1 — Generate MONTHLY_PL report:
    1. Navigate to the Reports section in the client dashboard
    2. Select "Monthly P and L" (or MONTHLY_PL) report type
    3. Click Generate
    4. Expected: Streaming text appears incrementally in the UI
    5. Open browser DevTools > Network tab, filter for EventStream — verify SSE events:
       - First event: { reportId: "..." }
       - Multiple events: { text: "..." }
       - Final event: { done: true }
    6. Wait for generation to complete fully

    TEST 5.2 — Report reaches COMPLETED status:
    1. After generation finishes, verify the report shows as completed in the UI
    2. Optionally verify in Supabase Dashboard: report table, status column = "COMPLETED"

    TEST 5.3 — PDF export:
    1. For the completed report from 5.1, click the PDF download/export button
    2. Expected: A PDF file downloads
    3. Open the downloaded PDF — verify it contains the report content (not blank/error)

    TEST 5.4 — Create shared report link:
    1. For the completed report, click Share (or equivalent UI action)
    2. Expected: A URL is generated in format https://finclear.app/shared-report/{64-char-hex-token}
    3. Copy the URL

    TEST 5.5 — Shared link works unauthenticated:
    1. Open the shared report URL from 5.4 in an incognito/private browser window (no login)
    2. Expected: Report renders fully — readable content, no auth prompt

    TEST 5.6 — Invalid share token returns 404:
    1. Visit https://finclear.app/shared-report/invalidtoken123
    2. Expected: 404 page

    TEST 5.7 — Generate additional report type (optional but recommended):
    1. Generate at least one more report type (TAX_DEDUCTIONS, CASH_FLOW_FORECAST, or BUSINESS_HEALTH_SCORE)
    2. Expected: Generates and reaches COMPLETED status
  </action>
  <verify>User reports PASS/FAIL/BLOCKED for Tests 5.1 through 5.7</verify>
  <done>AI report pipeline verified end-to-end: generate with SSE streaming, PDF export, and shared link all working (or failures documented).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Stripe Checkout + Subscription Management</name>
  <files>.planning/phases/02-production-testing/02-03-SUMMARY.md</files>
  <action>
    Guide the user through Stripe checkout and subscription management verification.

    PREREQUISITE CHECK: Verify in Vercel Dashboard that these env vars are set:
    - STRIPE_SECRET_KEY (test mode key starting with sk_test_)
    - STRIPE_WEBHOOK_SECRET (whsec_...)
    - NEXT_PUBLIC_STRIPE_STARTER_PRICE_ID
    - NEXT_PUBLIC_STRIPE_PROFESSIONAL_PRICE_ID
    - NEXT_PUBLIC_STRIPE_BUSINESS_PRICE_ID

    Execute each test on https://finclear.app while signed in as the CLIENT user.

    TEST 6.1 — Pricing page loads:
    1. Navigate to https://finclear.app/pricing
    2. Expected: Three plan tiers displayed (Starter, Professional, Business) with correct prices ($19, $49, $99)

    TEST 6.2 — Stripe checkout flow:
    1. Click Subscribe on the STARTER plan ($19)
    2. Expected: Redirected to Stripe Checkout page
    3. Enter test card: 4242 4242 4242 4242, expiry: 12/28, CVC: 123, ZIP: 12345
    4. Complete checkout
    5. Expected: Redirected back to /dashboard/client (possibly with ?upgraded=true)

    TEST 6.3 — Verify subscription in Stripe Dashboard:
    1. Open Stripe Dashboard > Customers or Payments
    2. Find the test payment from 6.2
    3. Expected: Checkout session completed, subscription active

    TEST 6.4 — Verify webhook fired:
    1. In Stripe Dashboard > Developers > Webhooks
    2. Check the webhook endpoint events
    3. Expected: checkout.session.completed event was sent and received (status 200)

    TEST 6.5 — Verify subscription in database:
    1. Check Supabase Dashboard > Table editor > subscription table
    2. Expected: Row exists with stripeCustomerId, stripeSubscriptionId, plan = STARTER, status = ACTIVE

    TEST 6.6 — Billing portal access:
    1. In the client dashboard, find the billing/subscription management section
    2. Click to manage subscription (should open Stripe Billing Portal)
    3. Expected: Stripe Billing Portal loads showing current subscription
    4. If 400 "No active subscription" error: webhook may not have fired — see Pitfall 4
  </action>
  <verify>User reports PASS/FAIL/BLOCKED for Stripe env vars check and Tests 6.1 through 6.6</verify>
  <done>Stripe payment pipeline verified: checkout completes, webhook fires, subscription created in DB, billing portal accessible (or failures documented).</done>
</task>

</tasks>

<verification>
- At least one AI report generated, PDF downloaded, and share link functional
- Stripe checkout completed with test card and subscription visible in both Stripe Dashboard and Supabase
- Billing portal accessible for subscribed user
</verification>

<success_criteria>
- All 13 test cases executed with documented results
- AI report pipeline verified end-to-end (generate -> PDF -> share)
- Stripe payment pipeline verified end-to-end (checkout -> webhook -> subscription -> portal)
- Any failures have specific error messages and reproduction steps
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-testing/02-03-SUMMARY.md` with all test results.
</output>
